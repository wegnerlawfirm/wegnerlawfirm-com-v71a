<div id="form-container">
    <form class="contact-us-form" id="form-contact">
        <div class="row">
            <div class="col">
                <label>
                    Name
                    <span class="contact-form-required">*</span>
                </label>
                <input class="contact-input" id="name" name="user_name" />
            </div>
        </div>

        <div class="row">
            <div class="col">
                <label> Phone </label>
                <input class="contact-input" id="phone" name="user_phone" />
            </div>

            <div class="col">
                <label>
                    Email
                    <span class="contact-form-required">*</span>
                </label>
                <input class="contact-input" id="email" name="user_email" />
            </div>
        </div>

        <div class="row">
            <div class="col">
                <label> Preferred contact method</label>
                <select class="contact-input" name="user_preferance">
                    <option>Select the best way to reach you.</option>
                    <option>Phone call</option>
                    <option>Text message</option>
                    <option>Email</option>
                    <option>No preferance</option>
                </select>
            </div>
        </div>

        <div class="row">
            <div class="col">
                <label>
                    Message
                    <span class="contact-form-required">*</span>
                </label>
                <textarea rows="5" id="message" name="user_message"></textarea>
            </div>
        </div>
        <div class="row">
            <div class="col btn-col">
                <input
                    type="submit"
                    value="Submit"
                    id="submit-btn"
                    class="form-btn"
                />
            </div>
        </div>
    </form>
    <div id='confirmation-form'>
        <div class="confirmation-content">
            <div class="confirmation-title">
                Form Submitted
            </div>
            <p class="content-text confirmation-text">
                We will contact you shortly. If you would like to speak to an us sooner, please call or email us.
            </p>
        </div>
    </div>
</div>

<script
  type="text/javascript"
  src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"
></script>
<script type="text/javascript">
  (function () {
    // https://dashboard.emailjs.com/admin/account
    emailjs.init({
      publicKey: "c9ADuJ6KZu1K5pqNB",
    });
  })();
</script>
<script type="text/javascript">
  const btn = document.getElementById("submit-btn");
  const form = document.getElementById("form-contact");
  const confirmation = document.getElementById("confirmation-form");

  form.addEventListener("submit", function (event) {
    event.preventDefault();

    btn.value = "Sending...";

    const serviceID = "default_service";
    const templateID = "template_fnjrc7h";
    form.style.display = "none";
    confirmation.style.display = "flex";

    emailjs.sendForm(serviceID, templateID, this).then(
      () => {
        btn.value = "Send Email";
        alert("Sent!");
      },
      (err) => {
        btn.value = "Send Email";
        alert(JSON.stringify(err));
      }
    );
  });
</script>


<style>
    #confirmation-form{
        display: none;
        justify-content: center;
        width: 100%;
        max-width: 350px;
        margin: 0 auto;

    }
    .confirmation-title{
        color: var(--logoColor);
        font-weight: 500;
    }
    .confirmation-content{
        text-align: center;
    }

    .col {
        padding: 0.5rem;
        width: 100%;
    }

    textarea {
        resize: none;
    }

    label {
        font-size: 0.9em;
        padding-left: 3px;
    }
    .form-btn {
        background-color: var(--altTitleColor);
        color: white;
    }

    .row {
        display: flex;
        flex-direction: column;

        @media(min-width: 768px){
            flex-direction: row;
        }
    }

    input,
    select,
    textarea {
        width: 100%;
        border: none;
        padding: 0.3rem;
        border-radius: 7px;
        min-height: 2rem;
        font-family: var(--primaryFont);
        font-size: 1rem;
    }

    @media (min-width: 768px) {
        .form-btn {
            width: 50%;
            margin-left: auto;
            max-width: 550px;
        }

        .btn-col{
            text-align: right;
        }

        #form-contact {
            border-radius: 14px;
            padding: 14px;
            margin-bottom: 3rem;
            background-color: #f8f8f8;
        }
    }
</style>
